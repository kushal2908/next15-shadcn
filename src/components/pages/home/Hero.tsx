'use client';
import { Button } from '@/components/ui/button';
import Image from 'next/image';

type Props = {};

export default function Hero({}: Props) {
    return <div className="flex flex-col justify-center items-center h-screen"></div>;
}
